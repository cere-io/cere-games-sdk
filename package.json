{
  "name": "cere-games-sdk",
  "version": "0.3.0",
  "private": true,
  "devDependencies": {
    "eslint": "^8.34.0",
    "prettier": "^2.8.4",
    "typescript": "^4.9.5",
    "wait-on": "^7.0.1"
  },
  "scripts": {
    "start": "npm start --workspace=@cere-games-sdk/examples",
    "playground": "npm start --workspace=@cere-games-sdk/playground",
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint --quiet '**/*.{ts,tsx,js,jsx}'",
    "test": "npm run test --workspaces --if-present",
    "test:ci": "npm run test:ci --workspaces --if-present",
    "sdk:publish": "npm publish --workspace=@cere/games-sdk-ui --workspace=@cere/games-sdk --access public"
  },
  "workspaces": [
    "packages/*",
    "tests/*",
    "examples",
    "playground"
  ]
}
